apiVersion: apps/v1
kind: Deployment
metadata:
  name: zzzzvolume-test
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zzzzvolume-test
  template:
    metadata:
      labels:
        app: zzzzvolume-test
    spec:
      containers:
      - name: zzzzvolume-test
        image: httpd:latest
        volumeMounts:
        - name: zzzzvolv
          mountPath: /usr/local/apache2/htdocs
        ports:
          - containerPort: 80
      volumes:
      - name: zzzzvolv
        persistentVolumeClaim:
            claimName: local-path-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: zzzzmyvolumn-nginx-service
spec:
  type: NodePort
  selector:
    app: zzzzvolume-test
  ports:
    - protocol: TCP
      port: 80
---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: zzzzmyvolumn-nginx-service
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - http:
        paths:
          - path: /mccoy
            pathType: Prefix
            backend:
              service:
                name: zzzzmyvolumn-nginx-service
                port:
                  number: 80
